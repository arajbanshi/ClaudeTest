[project]
name = "claudetest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "arajbanshi", email = "ashish.rajbanshi@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "claude-agent-sdk>=0.2.148",
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
claudetest = "claudetest:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
